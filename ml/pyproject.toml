[project]
name = "studioflow-ml"
version = "0.1.0"
description = "StudioFlow ML service for audio separation using Demucs"
requires-python = ">=3.10"

[project.scripts]
separate = "src.separate:main"
